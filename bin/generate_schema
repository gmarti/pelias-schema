#!/usr/bin/env node

const fs = require('fs');
const path = require('path');

process.chdir(path.join(__dirname, '..'));

const schema = require('../schema');
fs.writeFileSync('pelias-schema.json', JSON.stringify(schema, null, 2));
